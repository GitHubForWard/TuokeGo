<!--components/swiper/swiper.wxml-->
 <swiper autoplay indicator-dots circular interval="3000" class="index_swiper">
    <swiper-item wx:for="{{ swiperList }}" wx:key="goods_id">
      <navigator>
        <image mode="widthFix" src="{{ item.image_src }}" />
      </navigator>
    </swiper-item>
</swiper>
